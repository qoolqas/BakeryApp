<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.q.bakeryapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="n">
        <activity android:name=".ui.manage.DeleteActivity"></activity>
        <activity android:name=".ui.manage.EditActivity" />
        <activity android:name=".ui.manage.CreateActivity" />
        <activity android:name=".ui.manage.ManageActivity" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/AppTheme.Material" />
        <activity android:name=".ProfileActivity" />
        <activity
            android:name=".PembayaranActivity"
            android:theme="@style/AppTheme.Material" />
        <activity android:name=".ui.ProdukActivity" />
        <activity android:name=".DetailActivity" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.Material">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>